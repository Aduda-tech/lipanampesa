services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    environment:
      - DB_FILE=/data/shop.db
    volumes:
      - shopdata:/data
    restart: unless-stopped

volumes:
  shopdata:
